/** The boxes on hc5_optical, from the shared HC5 descriptions. */
import { hc5Fields } from './hc5-common.fields.farsi.js';

export default hc5Fields({
  paidFor: 'تست چشم، عینک یا لنز',
  who: 'خدمات چشم گرفته است',
});
