/** The boxes on hc5_dental, from the shared HC5 descriptions. */
import { hc5Fields } from './hc5-common.fields.farsi.js';

export default hc5Fields({
  paidFor: 'هزینهٔ دندان‌پزشکی',
  who: 'درمان دندان NHS شده است',
});
